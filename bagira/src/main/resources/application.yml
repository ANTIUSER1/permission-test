spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://192.168.2.100:5432/bagira
    username: postgres
    password: 123
    driverClassName: org.postgresql.Driver
    # ANOTHER SCHEMA
    #    hikari:
    #      schema: livedata

